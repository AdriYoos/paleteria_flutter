name: hel_arte
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.0.0        # Agregar Dio
  image_picker: ^0.8.5+3  # Agregar image_picker
  path: ^1.8.0           # Agregar path
  intl: ^0.18.1
  pdf: ^3.8.1
  printing: ^5.7.0
  excel: ^2.0.0-null-safety-0
  path_provider: ^2.0.11  # Para guardar el archivo en el dispositivo
  open_file: ^3.2.1       # Para abrir el archivo después de generarlo
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
